package binary_search;

public class BinarySearch {

    public static int binarySearchAlgorithm(int[] arr, int number) {
        int[] temp = {};
        for (int i = 0; i < arr.length; i++) {

             temp = new int[]{arr[i]};
             arr = new int[]{temp[0]};
        }

        return -5;
    }
}
